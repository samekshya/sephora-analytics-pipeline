numpy==2.5.1
pandas==3.0.3
psycopg2-binary==2.9.12
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
six==1.17.0
tzdata==2026.3

# --- Dashboard ---
# Pinned to the versions actually installed and verified on this machine,
# not to whatever was current when the file was written.
streamlit==1.61.1
plotly==6.9.0
statsmodels==0.14.6   # required by plotly's trendline="ols"
kaleido==1.3.0        # static image export for presentation/build_charts.py
